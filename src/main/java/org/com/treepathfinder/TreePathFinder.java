package org.com.treepathfinder;


import java.util.List;

public class TreePathFinder {

    //Write a method, pathFinder, that takes in the root of a binary tree and a target value.
    // The method should return an array representing a path to the target value.
    // If the target value is not found in the tree, then return null.
    //
    //You may assume that the tree contains unique values.
    public static <T> List<T> pathFinder(Node<T> root, T target) {
        // todo
        return null;
    }

    public static void run() {
        // this function behaves as `main()` for the 'run' command
        // you may sandbox in this function , but should not remove it
    }
}
